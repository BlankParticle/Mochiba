import { LoginForm } from "@web/components/blocks/login-form";

export default function LoginPage() {
  return (
    <div className="flex h-full w-full flex-col items-center justify-center">
      <LoginForm />
    </div>
  );
}
